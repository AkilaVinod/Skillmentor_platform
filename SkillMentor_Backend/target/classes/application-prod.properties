# Additional DB Configurations
# ===
spring.jpa.hibernate.ddl-auto=update
logging.level.root=ERROR

# Authentication Configuration (Clerk)
# ===
auth.validator.type=clerk

# Prod
clerk.jwks.url=${CLERK_JWKS_URL:https://hot-cowbird-43.clerk.accounts.dev/.well-known/jwks.json}
clerk.secret.key=${CLERK_SECRET_KEY:sk_test_j0CsHD8qkKeKKOgDUJzvHIQsyIOhCtlZa962A4rDrp}

# CORS set to your Vercel frontend URL on Render once deployed
# e.g. CORS_ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:3001
# application.properties

# CORS allowed origins
# Development & Production
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://skillmentor-frontend-one.vercel.app,http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:5173}

app.payment.upload-dir=${PAYMENT_UPLOAD_DIR:uploads/payment-proofs}
